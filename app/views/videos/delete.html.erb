<%= link_to("<< Back", {:controller => 'dashboard', :action => 'index'}, :class => 'back-link btn btn-default') %>

<br />

<section id="delete-video">

	<h3>Delete Video</h3>

	<%= form_for(:video, :url => {:controller => 'videos', :action => 'destroy', :id => @videos.id}) do |t| %>

		<p>Are you sure you want to delete?</p>

		<p class="reference-name"><%= @videos.title %></p>

		<%= submit_tag("Delete Video", :class => "btn btn-primary") %>

	<% end %>

</section>

<br />
