<%= link_to("<< Back", {:controller => 'dashboard', :action => 'index'}, :class => 'back-link btn btn-default') %>

<br />

<section id="delete-news-item">

	<h3>Delete News Item</h3>

	<%= form_for(:news_item, :url => {:controller => 'news_items', :action => 'destroy', :id => @news_item.id}) do |t| %>

		<p>Are you sure you want to delete?</p>

		<p class="reference-name"><%= @news_item.title %></p>

		<%= submit_tag("Delete News Item", :class => "btn btn-primary") %>

	<% end %>

</section>

<br />
